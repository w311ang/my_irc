version: '3.8'

services:
  irc-backend:
    image: inspircd/inspircd-docker:latest
    container_name: my-irc-server
    ports:
      - "6667:6667"
    restart: unless-stopped
    networks:
      - shared-net

  qwebirc-frontend:
    image: tob1as/qwebirc:latest
    container_name: my-qwebirc
    ports:
      - "9090:9090"
    depends_on:
      - irc-backend
    volumes:
      - ./config.py:/qwebirc/config.py:ro
    restart: unless-stopped
    networks:
      - shared-net

  file-host:
    image: andreimarcu/linx-server:latest
    container_name: my-file-host
    ports:
      - "8080:8080"
    volumes:
      - ./linx-data:/data
    command: 
      - "-bind=0.0.0.0:8080"
      - "-sitename=Retro Storage"
      - "-allowpublicupload=true"
    restart: unless-stopped

networks:
  shared-net:
    external: true
    name: irc-shared-net
